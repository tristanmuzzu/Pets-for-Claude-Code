{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions the overlay window needs. Everything else the app does runs in Rust.",
  "windows": ["pet"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
